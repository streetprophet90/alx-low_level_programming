#include <stdio.h>

/**
 * main - prints alphabet in lowercase and uppercase followed by a new line
 *
 * Return: 0
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);

		putchar ('\n');
	/* print uppercase letters */

	for (ch = 'A'; ch <= 'Z', ch++)
		putchar(ch);

		putchar (\n') ;
		return (0);
}
